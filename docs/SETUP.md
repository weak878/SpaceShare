# Setup Guide

## Frontend Setup (React Native)

```bash
cd frontend
npm install
npm start
```

## Backend Setup (Python/Flask)

```bash
cd backend
pip install -r requirements.txt
python main.py
```

## Database Setup

Make sure PostgreSQL is installed and running, then update `.env` file with your database URL.
